{{- if .Values.rateLimit.enabled }}
---
# Scanner Rate Limiting Middleware
# Protects scanner endpoints (Container Security) from abuse
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Values.rateLimit.scanner.name | default "tes-scanner-rate-limit" }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: traefik
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: middleware
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  rateLimit:
    average: {{ .Values.rateLimit.scanner.average }}
    burst: {{ .Values.rateLimit.scanner.burst }}
    period: {{ .Values.rateLimit.scanner.period | default "1s" }}
    sourceCriterion:
      ipStrategy:
        depth: {{ .Values.rateLimit.ipStrategy.depth | default 1 }}
        {{- if .Values.rateLimit.excludedIPs }}
        excludedIPs:
        {{- range .Values.rateLimit.excludedIPs }}
          - {{ . }}
        {{- end }}
        {{- end }}
{{- end }}
